client:
  url: "https://localhost:12345"
  timeout: 10000
  user_agent: ""

cargo_queries:

  - gbvsr:
    - MoveData_GBVSR:
      - input
      - name
      - note : parseWikitext

    - gbvsrCharacters:
      - name
      - health