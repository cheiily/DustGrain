client:
  url: "https://dustloop.com/api.php"
  timeout: 10000
  user_agent: ""

cargo_queries:

  - gbvsr:
    - MoveData_GBVSR:
      - input
      - name
      - note : parseWikitext

    - gbvsrCharacters:
      - name
      - health